# Changelog

## Unreleased

- Add the `one_main_zero_pr` repository hygiene observation: healthy repos have
  default branch `main` and zero open pull requests; in-progress work newer
  than 2 hours is left alone; stale extras are observations for
  `subactor/doctor-agent` and never authorize merge or branch deletion.

## 0.1.0-dev - 2026-08-12

- Start the standalone Git lifecycle standard.
